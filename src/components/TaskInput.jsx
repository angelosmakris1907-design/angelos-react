function TaskInput() {
  return (
    <section>
      <h2>Add Task</h2>

      <input
        type="text"
        placeholder="Enter a task..."
      />

      <button>Add</button>
    </section>
  );
}

export default TaskInput;